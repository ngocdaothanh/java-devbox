# Hello World

To compile `Hello.java` to `Hello.class`:
`javac Hello.java`

To run `main` method in class `Hello`
(`Hello.class` is in the current directory):
`java Hello`

To run `main` method in class `Hello`
(`Hello.class` is in the current directory):
`java -cp path/to/directory/containing/Hello.class Hello`
