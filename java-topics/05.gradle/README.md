# Gradle build tool

Popular Java build tools:
- Gradle (newer)
- Maven (older)

Build tools are for:
- Compiling Java files.
- Packaging (build) .jar files.
- Download dependency libraries.

## How to build this project

`./gradlew build`

## How to run this project

`./gradlew run`
