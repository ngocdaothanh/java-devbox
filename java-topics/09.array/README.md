# Arrays in Java

In Java, an array has a fixed size; unlike in JavaScript.
=> To create a new array, you need to know the size when you create an array!

## Create array with size

```java
int[] numbers = new int[3];
```

## Create array with initial values

```java
int[] numbers = {1, 5, 9};
```
